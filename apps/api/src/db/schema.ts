/**
 * Database schema — re-exported from @ops/db.
 *
 * All table definitions live in packages/db/src/schema.ts.
 * This file exists for backwards compatibility with imports in apps/api
 * that use relative paths (e.g., `import { events } from './schema.js'`).
 */
export * from '@ops/db'
